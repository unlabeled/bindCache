{
  "name": "bindcache",
  "version": "0.1.4",
  "description": "Cache/memoize result of bind function with its context or/and args (with shims)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre-commit": "lint-staged",
    "start": "webpack --watch"
  },
  "pre-commit": [
    "pre-commit"
  ],
  "lint-staged": {
    "*.@(js|jsx|ts|tsx)": [
      "prettier --write",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/unlabeled/bindCache.git"
  },
  "keywords": [
    "cache",
    "momoize",
    "bind"
  ],
  "author": {
    "name": "Timofey Tkachenko",
    "email": "ttimatika@gmail.com"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/unlabeled/bindCache/issues"
  },
  "homepage": "https://github.com/unlabeled/bindCache#readme",
  "dependencies": {
    "cachebind": "0.0.2"
  },
  "devDependencies": {
    "lint-staged": "^6.0.0",
    "pre-commit": "^1.2.2",
    "prettier": "^1.10.1",
    "webpack": "^3.x"
  },
  "peerDependencies": {
    "es6-map": "^0.1.5",
    "es6-weak-map": "^2"
  }
}
